package routes

import "github.com/gin-gonic/gin"

func AuthRoutes (superRoute *gin.RouterGroup) {
	// superRoute.POST("auth/token/generate/:type", GenerateToken)
	// superRoute.GET("auth/token/info/:type", DecodeToken)
	// superRoute.GET("auth/token/validate/:type", ValidateToken)
}